# torch CUDA builds only exist on PyTorch's own index; the +cu129 pin keeps pip
# from silently resolving the CPU-only wheel from PyPI.
--extra-index-url https://download.pytorch.org/whl/cu129

# UI
customtkinter==5.2.2

# Media
av==18.1.0
mutagen==1.47.0

# Link downloads (Instagram, Facebook, TikTok, YouTube).
#
# Deliberately a floor rather than a pin. These sites change what they serve every few
# weeks and yt-dlp tracks them release by release, so a pin here is a promise that
# downloads will stop working a few months after the build. Only the major-version
# boundary is guarded.
yt-dlp>=2026.8.19

# Pillow draws the provider marks on the download tiles, and CTkImage needs it to
# scale them for high-DPI displays.
pillow>=11.0

# Qwen3-ASR engines
accelerate>=1.0.0
huggingface_hub>=1.4.1
torch==2.9.0+cu129
transformers>=5.13.0

# Silero VAD (transcriber/assets/silero_vad_v6.onnx) used to split long audio
onnxruntime>=1.20
